// Merge two objects with matching keys

// Write a function that takes two objects
// Merge them
// If keys match, sum their values

function myFunction(a, b) {
  
}

console.log(
  myFunction(
    { a: 1, b: 2 },
    { b: 3, c: 4 }
  )
);

// { a: 1, b: 5, c: 4 }