// Add multiple elements to Set

// Write a function that takes a Set and an array
// Add all array elements to the Set
// Return the updated Set

function myFunction(set, arr) {
  
}

console.log(
  myFunction(
    new Set([1, 2]),
    [2, 3, 4]
  )
); // Set(4) {1, 2, 3, 4}

console.log(
  myFunction(
    new Set(['a']),
    ['b', 'c']
  )
); // Set(3) {'a', 'b', 'c'}