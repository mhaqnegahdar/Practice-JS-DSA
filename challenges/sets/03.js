// Get union of two sets

// Write a function that takes two Sets
// Return a new Set containing all unique values

function myFunction(set1, set2) {
  
}

console.log(
  myFunction(
    new Set([1, 2]),
    new Set([2, 3])
  )
); // Set(3) {1, 2, 3}

console.log(
  myFunction(
    new Set(['a']),
    new Set(['a', 'b'])
  )
); // Set(2) {'a', 'b'}