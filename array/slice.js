// Returns a shallow copy of the original array without changing the original array

const participants = ["Mohamad", "Max", "Jim", "Bob", "John", "Ahmad"];

const winners = participants.slice(0, 3);

console.log("Original array: ", participants);
console.log("Winners array: ", winners);
